- 所有数据库操作函数（如 select, insert）均接收 executor 实例作为第一个参数，实现逻辑与连接状态的解耦。
- WHERE 条件统一采用字典格式，支持简单值（自动等值匹配）或元组 `(operator, value)` 自定义比较逻辑。
- 批量插入根据数据量自动切换策略：<1000 条使用 executemany，≥10万条自动降级为 LOAD DATA INFILE 以提升性能。
- 查询结果格式化通过 `FetchConfig` 统一控制，支持 fetch_mode (all/one) 与 output_format (df/dict/list) 的组合配置。